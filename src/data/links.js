const originUrl = "/fe";

const links = {
  main: originUrl,
  trains: `${originUrl}/trains`,
  seats: `${originUrl}/seats`,
  passengers: `${originUrl}/passengers`,
  paymentOptions: `${originUrl}/paymentOptions`,
  confirmOrder: `${originUrl}/confirmOrder`,
  success: `${originUrl}/success`,
};

export default links;
